// Next.js uses the parent layout by default when this file is absent.
// Kept as a placeholder for future room-bill-specific layout needs.
export default function RoomBillLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
